# Requirements

Python version >=3.10
(*match statement*)

Libraries

*`os` is built-in*

```bash
pip install unidecode
```

# Usage

Just run `library_system.py` in `python_exercise`
